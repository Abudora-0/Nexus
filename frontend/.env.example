# Base URL of the Nexus backend API.
# Local dev leaves this unset (defaults to http://localhost:8000).
# For a deploy, set it to your hosted backend, e.g. https://nexus-api.onrender.com
VITE_API_URL=
